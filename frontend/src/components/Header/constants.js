/**
 * Constants for the Header component
 */

// Mobile breakpoint (matches with Tailwind md breakpoint)
export const MOBILE_BREAKPOINT = 768; // px

// Very small mobile breakpoint for extra compact layout
export const SMALL_MOBILE_BREAKPOINT = 400; // px

// Minimum width for profile image (to prevent layout shift)
export const PROFILE_IMAGE_MIN_WIDTH = '32px'; 