ref = 'cat on the mat'; hyp = 'a cat on the mat'; # Calculate character matches; common_chars = 0; hyp_copy = hyp; for c in ref: if c in hyp_copy: common_chars += 1; hyp_copy = hyp_copy.replace(c, '', 1); p_char = common_chars/len(hyp); r_char = common_chars/len(ref); # Calculate bigram matches; ref_bi = [ref[i:i+2] for i in range(len(ref)-1)]; hyp_bi = [hyp[i:i+2] for i in range(len(hyp)-1)]; common_bi = 0; hyp_bi_copy = hyp_bi.copy(); for bi in ref_bi: if bi in hyp_bi_copy: common_bi += 1; hyp_bi_copy.remove(bi); p_bi = common_bi/len(hyp_bi); r_bi = common_bi/len(ref_bi); # Calculate chrF; avg_p = (p_char + p_bi)/2; avg_r = (r_char + r_bi)/2; f1 = 2*avg_p*avg_r/(avg_p+avg_r); print(f'chrF = {f1:.2f}')
